# content/fyp/

FYP project entries are registered in `lib/data.ts` as `FypMeta[]`.

To add a new FYP project, run:

```bash
npm run new:fyp
```

This scaffolds a new entry in the data file and creates the detail page under `app/fyp/[slug]/`.
