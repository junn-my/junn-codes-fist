# content/subjects/

Each subject page lives in `app/subjects/[CODE]/page.tsx`.

To add a new subject, run:

```bash
npm run new:subject
```

This scaffolds a new page under `app/subjects/[CODE]/` pre-filled with the subject metadata.
